import React from "react";
interface ResetPasswordProps {
    name: string;
    resetLink: string;
}
declare const ResetPasswordEmail: React.FC<ResetPasswordProps>;
export default ResetPasswordEmail;
